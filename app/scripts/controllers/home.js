'use strict';

/**
 * @ngdoc function
 * @name ngApp.controller:HomeCtrl
 * @description
 * # HomeCtrl
 * Controller of the ngApp
 */
angular.module('ngApp').controller('HomeCtrl', ['$rootScope', '$scope', 'user', '$state', function ($rootScope, $scope, user, $state) {
        
    }]);